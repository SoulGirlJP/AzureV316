function enter(pi) {
	//if (pi.isQuestStarted(22010) || pi.isQuestCompleted(22010)){
		pi.warp(100030310);
	//} else {
	//	pi.playerMessage("Cannot enter the Lush Forest without a reason.");
	//}
	return true;
}